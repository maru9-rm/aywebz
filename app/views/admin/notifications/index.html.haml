= render 'commons/header'

.container.text-center
    %h1.section-title.text-center お知らせ作成ページ

    - @notifications.each do |notification|
        %h1
            #{notification.title} by #{notification.user.displayname}

        %h2.p-3.pb-5
            = notification.content

    = link_to "新しいお知らせを作る", new_admin_notification_path